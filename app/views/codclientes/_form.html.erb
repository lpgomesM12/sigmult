<%= form_for(codcliente) do |f| %>
  <% if codcliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(codcliente.errors.count, "error") %> prohibited this codcliente from being saved:</h2>

      <ul>
      <% codcliente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :numr_codigo %>
    <%= f.number_field :numr_codigo %>
  </div>

  <div class="field">
    <%= f.label :empresa_id %>
    <%= f.text_field :empresa_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
