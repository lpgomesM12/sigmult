<%= form_for(caixa) do |f| %>
  <% if caixa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(caixa.errors.count, "error") %> prohibited this caixa from being saved:</h2>

      <ul>
      <% caixa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data_abertura %>
    <%= f.datetime_select :data_abertura %>
  </div>

  <div class="field">
    <%= f.label :data_fechamento %>
    <%= f.datetime_select :data_fechamento %>
  </div>

  <div class="field">
    <%= f.label :valr_caixa %>
    <%= f.text_field :valr_caixa %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
